class Track {
    constructor(title, version, artist, album, length) {
        this.title = title;
        this.version = version;
        this.artist = artist;
        this.album = album;
        this.length = length;
    }
}

module.exports = Track;